version: "3.9"
services:
  php:
    build: .
    container_name: desarrollo-gestion-posgrado
    user: "${UID}:${GID}"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      APP_ENV: dev
      HOME: /app/home
    tty: true
