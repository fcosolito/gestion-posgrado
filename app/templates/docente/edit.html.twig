{% extends 'base.html.twig' %}

{% block title %}Editar Docente{% endblock %}

{% block body %}
<div class="p-2">
    {{ include('docente/_form.html.twig', {'button_label': 'Guardar'}) }}

    <div class="row p-2">
        <div class="col-auto">
            <a class="btn btn-secondary" href="{{ path('app_docente_index') }}">Volver</a>
        </div>
        <div class="col-auto">
            {{ include('docente/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
