{% extends 'base.html.twig' %}

{% block title %}Carrera index{% endblock %}

{% block body %}
    <h1>Carrera index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>NroOrdenanza</th>
                <th>NroImplementacion</th>
                <th>CantidadCuotas</th>
                <th>PrecioInscripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for carrera in carreras %}
            <tr>
                <td>{{ carrera.id }}</td>
                <td>{{ carrera.nombre }}</td>
                <td>{{ carrera.nroOrdenanza }}</td>
                <td>{{ carrera.nroImplementacion }}</td>
                <td>{{ carrera.cantidadCuotas }}</td>
                <td>{{ carrera.precioInscripcion }}</td>
                <td>
                    <a href="{{ path('app_carrera_show', {'id': carrera.id}) }}">show</a>
                    <a href="{{ path('app_carrera_edit', {'id': carrera.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_carrera_new') }}">Create new</a>
{% endblock %}
