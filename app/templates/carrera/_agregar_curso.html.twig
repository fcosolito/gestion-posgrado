{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('/styles/carrera/agregar_curso.css') }}">
{% endblock %}

<div class="agregar-curso">
    <div class="lista-cursos-restantes">
        <h3>Cursos restantes</h3>
            <ul>
                {% for curso in cursosRestantes %}
                    <li>{{ curso.nombre }}</li>
                                {% endfor %}
            </ul>
        </div>
    <div>
        <h3>Cargar Curso</h3>
    </div>
</div>