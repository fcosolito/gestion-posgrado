{% extends 'base.html.twig' %}

{% block title %}Registrar Pago{% endblock %}

{% block body %}

<div class="p-2">
    {{ include('pago/_form.html.twig', {'button_label': "Registrar"}) }}

    <div class="m-3">
        <a class="btn btn-secondary" href="{{ path('app_pago_index') }}">Volver</a>
    </div>
</div>
{% endblock %}
