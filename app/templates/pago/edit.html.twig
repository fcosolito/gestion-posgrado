{% extends 'base.html.twig' %}

{% block title %}Edit Pago{% endblock %}

{% block body %}
<div class="p-2">
    {{ include('pago/_form.html.twig', {'button_label': 'Guardar'}) }}

    <div class="row p-2">
        <div class="col-auto">
            <a class="btn btn-secondary" href="{{ path('app_pago_index') }}">Volver</a>
        </div>
        <div class="col-auto">
            {{ include('pago/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
