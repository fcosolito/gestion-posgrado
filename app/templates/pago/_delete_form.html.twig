<form method="post" action="{{ path('app_pago_delete', {'id': pago.id}) }}" onsubmit="return confirm('Esta seguro que quiere eliminar este pago?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pago.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
